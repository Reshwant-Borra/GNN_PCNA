# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.installed.cfg
*.egg

# Test / coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/
.hypothesis/
coverage.xml
*.cover

# Virtual environments
.venv/
.venv_*/
venv/
env/
ENV/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
desktop.ini

# JavaScript scratch dependencies generated during local audits
node_modules/
package.json
package-lock.json

# Workflow report outputs (timestamped, regenerated on every run)
reports/research_os/

# Obsidian vault placeholder (kept locally, not part of the Python package)
ResearchOS/

# Large MD trajectories / checkpoints — should never be in git
*.dcd
*.xtc
*.trr
*.chk
*.h5
*.pt
*.pth
*.ckpt
*.npy

# MD validation package — runtime artifacts (generated on the friend's machine)
md_validation_4070/outputs/
md_validation_4070/preflight_outputs*/
md_validation_4070/run_*.log
md_validation_4070/.pcna_pipeline.sh
md_validation_4070/**/__pycache__/

# --- recreatable artifacts: environments, downloaded third-party data, run outputs ---
# Environments (rebuild from pyproject.toml / md_validation_4070/environment.yml)
.venv_mdanalysis/
.obsidian/

# Third-party datasets — re-download, do not vendor (scPDB 2.7G, CryptoBench 1.1G)
data/phase3_labels/
data/raw_intake/cryptobench/

# Literature corpus: PDFs are re-fetchable from corpus_manifest.json (377M, 121 PDFs)
data/literature/pdfs/
data/literature/index/

# Crawl/run logs and MD run outputs (trajectories, solvated systems)
logs/
outputs/**/*.pdb
outputs/**/*.dcd

# Redundant archive of a directory already tracked in this repo
md_validation_4070.zip

# Downloaded PCNA structure crawl (29 MB of .cif.gz, re-fetchable from RCSB)
Desktop/GNN_PCNA/data/raw_intake/phase4_pcna_crawl/
