# Raw intake — large binary/CIF archives; quarantined data; never commit
data/raw_intake/

# API cache — large, regenerable, never commit
data/registries/.cluster_api_cache.json

# Personal documents — never commit
BORRA_#1_SARC.pdf

# Raw data directory
raw/

# Crawl knowledge bases — 288 MB, contain Windows-reserved filenames (prn.md etc.)
# Source leads only; not scientifically authoritative. Regenerable from crawl scripts.
crawls/

# Generated graph tensors — large binary artifacts; regenerate from CIFs + scripts
# Commit the manifest JSON only; .npz and _meta.json files are reproducible
data/graphs/*.npz
data/graphs/*_meta.json

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.egg-info/
dist/
build/

# Virtual environments
venv/
.venv/
env/

# IDE and OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Jupyter
.ipynb_checkpoints/

# pytest / coverage
.pytest_cache/
.coverage
htmlcov/
