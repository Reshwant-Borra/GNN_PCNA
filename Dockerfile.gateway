FROM python:3.11-slim

WORKDIR /workspace/GNN_PCNA

RUN apt-get update && apt-get install -y git curl && rm -rf /var/lib/apt/lists/*

RUN pip install --no-cache-dir "python-telegram-bot>=20" pyyaml openai magic-wormhole requests

CMD ["python", "agents/telegram_gateway.py"]