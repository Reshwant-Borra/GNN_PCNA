# PCNA Structures

Canonical local mmCIF references are under `data/raw_intake/pcna_structures/`. GNN scripts from graph-leakage-fix expect PDB files under `data/raw/`; those are generated/rebuildable or external artifacts unless restored by the production package.
