# Model Architecture

Canonical model code: `src/models/cryptic_gnn.py`. Main classes: `PocketGNN`, `PocketGNNXL`. XL uses spatial + sequential branches, gated fusion, virtual/global context, and sigmoid residue scores.
