# ── Core ML ───────────────────────────────────────────────────────────────────
# PyTorch — install the right build for your hardware FIRST (see SETUP.md)
torch>=2.1.0
torch-geometric>=2.4.0

# PyG sparse ops — install AFTER torch (version must match your torch + CUDA)
# See: https://pytorch-geometric.readthedocs.io/en/latest/install/installation.html
torch-scatter>=2.1.0
torch-sparse>=0.6.18

# ── Structural biology ─────────────────────────────────────────────────────────
biopython>=1.83
scipy>=1.11.0
numpy>=1.26.0

# ── ML utilities ──────────────────────────────────────────────────────────────
scikit-learn>=1.4.0
tqdm>=4.66.0

# ── Web crawling ──────────────────────────────────────────────────────────────
requests>=2.31.0
beautifulsoup4>=4.12.0

# ── UI ────────────────────────────────────────────────────────────────────────
streamlit>=1.35.0
matplotlib>=3.8.0
pandas>=2.1.0

# ── Molecular dynamics (optional — only needed for MD validation) ──────────────
# mdanalysis>=2.7.0
# prody>=2.4.0

# ── MCP server (optional — only needed for Claude Code integration) ────────────
# mcp>=1.0.0
